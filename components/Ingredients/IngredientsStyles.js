import { StyleSheet } from 'react-native'

const styles = () =>
    StyleSheet.create({
        container: {
            marginTop: 8,
            marginBottom: 48,
        },
    })

export default styles

import { StyleSheet } from 'react-native';

const styles = () =>
  StyleSheet.create({
    container: {
      flexDirection: 'row',
      marginTop: 12,
      marginBottom: 48,
      flexWrap: 'nowrap',
      alignSelf: 'flex-start',
    },
  });

export default styles;
